unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

set -g prefix C-Space
set -g mouse on
set -g default-terminal "tmux-256color"



# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v2.3.0'
set -g @catppuccin_flavor 'mocha' 
set -g @plugin 'christoomey/vim-tmux-navigator'

set-option -g status-position top

# Configure the catppuccin plugin
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"

# Load catppuccin
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux
# For TPM, instead use `run ~/.tmux/plugins/tmux/catppuccin.tmux`

# Fix low-visibility selection highlight in choose-tree (prefix + S / prefix + W).
# This is tmux's own native 'mode-style' option -- catppuccin/tmux does not wrap
# it with its own @catppuccin_* variable, so it's set directly here, using
# catppuccin's own theme color variables to stay visually consistent.
set -g mode-style "fg=#{@thm_crust},bg=#{@thm_green},bold"

# Make the status line pretty and add some modules
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
